#设置端口
server.port=8761
#设置服务名称
spring.application.name=spring_cloud_eureka_server

#表示当前项目不要注册到注册中心上
eureka.client.register-with-eureka=false

#表示当前项目不从注册中心上获取注册信息
eureka.client.fetch-registry=false

eureka.client.serviceUrl.defaultZone=http://localhost:${server.port}/eureka/